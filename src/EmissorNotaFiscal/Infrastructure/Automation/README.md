# Automation Boundary

This folder contains browser and RPA adapters.

Current concrete adapter:

- `ContractBasedAutomationEngine`: Playwright-based engine that interprets
  contract-defined automation steps, injects runtime values, and returns the
  downloaded PDF path from a successful run
