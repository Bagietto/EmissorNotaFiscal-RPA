# Storage Boundary

This folder contains file-system and local-storage adapters.

Current concrete adapter:

- `JsonConfigRepository`: asynchronous JSON-backed repository for billing
  configuration and automation recipe files stored on the local file system
